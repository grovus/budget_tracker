<li>
  <%= link_to category.name, category %>
  <% if current_user?(category.portfolio.user) %>
    | <%= link_to "delete", category, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>